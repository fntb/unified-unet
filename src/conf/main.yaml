defaults:
  - _self_
  - trainer: base
  - task@_global_: segmentation

output_dir: .cache
output_id: ${now:%Y-%m-%d_%H-%M-%S}

hydra:
  run:
    dir: ./${output_dir}/experiments/runs/${output_id}
  sweep:
    dir: ./${output_dir}/experiments/multiruns/${output_id}
    subdir: ${hydra.job.override_dirname}