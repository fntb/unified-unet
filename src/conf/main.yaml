defaults:
  - _self_
  - model: unet
  - data: mnist
  - trainer: base

output_dir: .cache

hydra:
  run:
    dir: ./${output_dir}/experiments/runs/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: ./${output_dir}/experiments/multiruns/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${hydra.job.override_dirname}